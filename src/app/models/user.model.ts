export interface UserInfo {
    id: string;
    firstName: string;
    lastName: string;
    userName: string;
    email: string;
    role: string;
    tier: string;
    active: boolean;
    authenticated: boolean;
    createdAt: string;
    updatedAt: string;
}